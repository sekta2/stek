1. [ ] `Config.Load()` нужно переделать чтобы конфиг создавался с заранее созданным пресетом настроек с комментариями(если файла конфига нет). (**желательно сделать когда будет много настроек**)

2. [ ] Добавить параметры `HoloPos` + `HoloAng` для авто-энтити ресурсов.
    1. [ ] Добавить использование `HoloPos` + `HoloAng` в рисовке авто-энтити.

3. [ ] Добавить создание объекта инвентаря у игрока при спавне.

4. [ ] Перенести значки ресурсов(+уменьшенные версии) из JMod в STek.
	 1. [ ] Сделать функцию подбора значка по размеру(по указанному размеру подбирается либо уменьшенная версия(32x32), либо нормальная(128x128)).
	 2. [ ] **(если сделан 4.1)** - добавить в класс ресурса вторую опцию, которая содержит id масштабируемого значка, и добавить использование функции из 4.1 в новый метод, пр.: :GetSizedIcon()
	       **(если не сделан 4.1)** - поменять пути к иконкам в ресурсах

---

Если хотите взять задачу, то проверьте что нет существующей ветки с выполнением этой задачи, создайте ветку с названием задачи(лучше номером, типа: `task-1`).

В ветке сделайте все нужные изменения и поставьте крестик в квадратных скобках в задаче.